
server.port=8083

spring.datasource.url=jdbc:mysql://localhost:3306/authnexus
spring.datasource.username=root
spring.datasource.password=monu@2003
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.springframework.secuirity=DEBUG

# JWT CONFIGURATION
security.jwt.secret=${JWT_SECRET:saiofal,vkasfbewqwpofhjashofjaswfpofjasfvbafop[hfifonfo[shjfwefjifslajsfas]}
security.jwt.issuer=${JWT_ISSUER:api.authnexus.com}
security.jwt.access-ttl-seconds=${JWT_ACCESS_TTL_SECONDS:3600}
security.jwt.refresh-ttl-seconds=${JWT_REFRESH_TTL_SECONDS:86400}
security.jwt.refresh-token-cookie-name=${JWT_REFRESH_COOKIE_NAME:refreshToken}
# COOKIE CONFIGURATION
security.jwt.cookie-secure=${JWT_COOKIE_SECURE:true}
security.jwt.cookie-http-only=${JWT_COOKIE_HTTP_ONLY:true}
security.jwt.cookie-same-site=${JWT_COOKIE_SAME_SITE:lax}
security.jwt.cookie-domain=${JWT_COOKIE_DOMAIN:localhost}


#OAUTH2
spring.security.oauth2.client.registration.google.client-id=${google-client-id:testId}
spring.security.oauth2.client.registration.google.client-secret=${google-client-secret:testSecret}
spring.security.oauth2.client.registration.google.scope=openid,profile,email



# mysql dialect auto after spring 3.0.0, so no need to specify it explicitly.

#hikari pool // most important for performance
#spring.datasource.hikari.pool-name=AuthNexusHikariCP
#spring.datasource.hikari.maximum-pool-size=10
#spring.datasource.hikari.minimum-idle=5
#spring.datasource.hikari.idle-timeout=30000
#spring.datasource.hikari.connection-timeout=20000
#spring.datasource.hikari.max-lifetime=1800000
#spring.datasource.hikari.validation-timeout=5000
#spring.datasource.hikari.leak-detection-threshold=0
#spring.datasource.hikari.initialization-fail-timeout=-1

